{
  "name": "entrega1codigo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && tsc && npm run copy-files",
    "copy-files": "copyfiles --error --up 1 src/**/*.ejs dist",
    "start": "npx nodemon --exec 'npm run build && node dist/server.js' --watch src -e ts,ejs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "copyfiles": "^2.4.1",
    "nodemon": "^2.0.14",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "ejs": "^3.1.8",
    "express": "^4.18.1"
  }
}
